{
  "name": "render-discord-query-forwarder",
  "version": "1.0.0",
  "private": true,
  "description": "A small Render-ready service that forwards an _bt query value to Discord.",
  "main": "server.js",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0"
  }
}
